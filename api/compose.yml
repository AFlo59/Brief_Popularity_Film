version: '3.8'
services:
  api:
    container_name: api
    # image: 
    build: .
    env_file:
      - .env
    command: "uvicorn main:app --host 0.0.0.0 --port 8001 --reload"
    volumes:
      - .:/app
    ports:
      - 8001:8001
